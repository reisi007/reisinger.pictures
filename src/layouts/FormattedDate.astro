---
interface Props {
  date: Date;
}

const {date} = Astro.props;
---

<time class="inline" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('de-AT', {
      year: 'numeric',
      month: 'long',
      day: 'numeric',
    }).trim()
  }
</time>
