---
import ImageGallery from "./ImageGallery.astro";
import { GalleryLayout } from "./GalleryLayout";

interface Props {
  sorted: string[],
  layout?: GalleryLayout
}

const { sorted: images, layout } = Astro.props;
---
{images.length > 0 &&
<ImageGallery images={images} layout={layout} />}
